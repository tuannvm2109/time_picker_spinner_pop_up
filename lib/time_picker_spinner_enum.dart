part of 'time_picker_spinner_pop_up.dart';

enum PressType {
  longPress,
  singlePress,
}

enum PositionType {
  top,
  bottom,
  right,
  left,
  overlap,
}

enum TimePickerSpinnerLayoutId {
  timePicker,
  buttonCancel,
  buttonConfirm,
}
